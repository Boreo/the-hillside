---
import { FACT_ICON_PATHS, factIconHtml } from "../lib/fact-icon-paths.mjs";

interface Props {
  name: keyof typeof FACT_ICON_PATHS;
}
const { name } = Astro.props;
---

<Fragment set:html={factIconHtml(name)} />
